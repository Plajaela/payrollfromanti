# 📚 เอกสารสรุประบบ (Project Documentation)

## 📌 ภาพรวม (Overview)
โปรเจกต์นี้เป็น Web Application สำหรับจัดการทรัพยากรบุคคล การลงเวลาทำงาน และการคิดเงินเดือน/ค่าแรง (Payroll) ของพนักงานหรือช่างในระบบ มีฟังก์ชันครอบคลุมตั้งแต่การบันทึกเวลาเข้า-ออก, การคำนวณโอที (OT), หักมาสาย, ค่ารถ, ค่าทางด่วน, การจัดการเบิกเงินล่วงหน้า, เงินประกันสะสม และการสรุปยอดเพื่อนำไปใช้งานต่อ (คัดลอกข้อความ, รูปภาพ, ส่งออก Excel)

**เทคโนโลยีหลักที่ใช้:**
- **Frontend Framework:** React 19 + Vite (TypeScript)
- **Styling/UI:** Tailwind CSS, Lucide React (Icons), Motion (Animations)
- **State Management & Backend:** Custom Hook (`useStore.ts`) ทำงานคู่กับ **Supabase** (PostgreSQL) พร้อมระบบ Realtime Subscriptions
- **Utilities:** `date-fns` (จัดการวันที่), `xlsx-js-style` (สร้าง Report Excel), `html-to-image` (สร้างรูปสลิป)

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)
> อัปเดตล่าสุด: 14 เมษายน 2026

```text
/payroll-web-app/payroll-main
├── package.json          # ไฟล์คอนฟิกโปรเจกต์ (Dependencies: @supabase/supabase-js, tailwindcss, motion, xlsx)
├── supabase/
│   └── migrations/       # SQL Migrations สำหรับอัปเดตโครงสร้าง Database
├── src/
│   ├── main.tsx          # จุดเริ่มต้นการ Render ของ React
│   ├── App.tsx           # หน้าต่างหลัก (Layout & Tab Routing)
│   ├── types.ts          # Type definitions ทั้งหมด เช่น Worker, DailyEntry, AdvancePayment
│   ├── useStore.ts       # 🧠 Core State Management: ควบคุมการทำงาน CRUD เชื่อมต่อ Supabase
│   ├── index.css         # สไตล์หลักและ Config ของ Tailwind
│   ├── components/       # Reusable Components
│   │   ├── SlipModal.tsx # โมดอลจัดการการดูและโยงสลิปโอนเงิน
│   │   └── ui.tsx        # UI Elements (Button, Input, Card, Modal, Toast)
│   ├── lib/
│   │   └── supabase.ts   # ตั้งค่า Supabase Client
│   └── pages/            # 🏠 หน้าจอการใช้งานหลัก
│       ├── DailyEntryPage.tsx # หน้าบันทึกเวลารายวัน (ลงเวลา, คำนวณรายรับรายวัน, อัปโหลดสลิป, ลางาน)
│       ├── DashboardPage.tsx  # หน้าสรุปสถิติภาพรวม
│       ├── ReportsPage.tsx    # หน้าออกรายงาน (คำนวณยอดสุทธิ, ส่งออกเป็น Excel)
│       ├── WalletPage.tsx     # หน้าระบบกระเป๋าเงิน (ประวัติเบิกล่วงหน้า, คืนเงินประกัน, ยอดยกมา)
│       └── WorkersPage.tsx    # หน้าจัดการรายชื่อพนักงาน (ตั้งค่าพื้นฐาน, กะการทำงาน, อัตราจ้าง)
```

---

## ✨ ฟีเจอร์ที่มีอยู่ในระบบปัจจุบัน (Current Features)

1. **การจัดการพนักงาน (Worker Management)**
   - เพิ่ม ลบ แก้ไข ข้อมูลพนักงาน เช่น ค่าแรงฐานรายวัน/รายเดือน, เวลาเริ่ม-เลิกงาน (Shift), มี/ไม่มีประกันสังคม
   - ตั้งค่ากฎพิเศษต่างๆ (เพดานเงินประกันสะสม, เรทการหักมาสาย)
2. **การบันทึกรายวัน (Daily Entry)**
   - บันทึกเข้าทำงาน/ออกงาน พร้อมระบบคำนวณสายและ OT อัตโนมัติ (สามารถ Edit ทับได้)
   - ฟีเจอร์ "ลางาน" ทุกประเภท (ลาป่วย, ลากิจ, ขาด, ขาดครึ่งวัน)
   - การจัดการค่าใช้จ่ายเพิ่มเติม (ทางด่วน, ค่ารถ, รายการหัก/เพิ่มอื่นๆ)
   - ระบบอัปโหลด "สลิป" หรือเอกสารยืนยัน พร้อมกับปุ่มแชร์ภาพ/คัดลอกข้อความ
3. **ระบบบัญชีพนักงาน (Wallet / Advances)**
   - บันทึกประวัติเบิกเงินล่วงหน้าและการคืนเงิน/หักหนี้
   - ระบบ **"เงินประกันสะสม"** ที่หักจากรายวัน พร้อมทั้งแสดงยอดเงินคืน (Guarantee Refund)
4. **สรุปและรายงาน (Reporting & Exports)**
   - สร้างตารางสรุปการจ่ายเงินในช่วงเวลาที่เลือกอย่างละเอียด
   - สามารถคัดลอกข้อความเป็นฟอร์แมตเพื่อนส่งแจกจ่ายได้ทันที
   - Export ฐานข้อมูลออกมาเป็นไฟล์ `.xlsx` (Excel) แยกแท็บตามชื่อช่าง
5. **Real-time Sync & Cache**
   - ดึงข้อมูลอัตโนมัติเมื่อมีการเปลี่ยนแปลงใน Database (Supabase Channels)
   - Migration ข้อมูลจาก LocalStorage สู่ Supabase อัตโนมัติถ้าเป็นการเปิดเครื่องใหม่ในระบบเดิม

---

## 📝 ความรับผิดชอบหลักของแต่ละไฟล์ (File Responsibilities)

- **`useStore.ts`**: รวม Logic การร้องขอ Database ทุกประเภท (fetch, insert, update, delete) มีการทำงานแบบ Optimistic UI (แสดงผลทันทีก่อนรอเซิร์ฟเวอร์ตอบกลับ)
- **`types.ts`**: ควบคุม Schema ป้องกันการส่งข้อมูลผิดประเภท ลดบั๊กขณะเขียนโค้ด
- **`DailyEntryPage.tsx`**: ใหญ่ที่สุดในแอพ จัดการตัวเลขรายวัน (ความซับซ้อนอยู่ที่ฟังก์ชันการนับ OT และ Copy Logic (`handleCopyAllDetailed`, `handleCopyAllSlips`))
- **`WalletPage.tsx`**: คำนวณหนี้และยอดเงินสะสม แยกแยะระหว่าง "รายการประวัติเบิก" และ "ประวัติคืนเงินประกัน" ไม่ให้ปนกัน
- **`ReportsPage.tsx`**: คำนวณสรุปผลยอดทั้งหมด เพื่อไม่ให้ Logic ตีกันกับ Daily Entry ไฟล์นี้จะใช้วิธีดึงตารางทั้งหมดมารวมกันใน `useMemo` อีกรอบ

---

## 🛠️ รายการการแก้ไขที่ผ่านมา (Recent Change Log)
*อิงจาก Git Log ล่าสุด:*
- **Fix:** (DailyEntryPage) แก้ไขความคลาดเคลื่อนของยอดรวมเงินประกัน โดยทำ strict number coercion และรวบ logic หักเงินให้เหมือนกับแอพส่วนอื่น
- **Feat:** เพิ่มเพดานการหักเงินประกัน (Flexible deposit deduction limit) ต่อช่างแต่ละคน (`add_guarantee_limit.sql`)
- **Fix:** (WalletPage) ช่วยให้ฟอร์มคืนเงินประกันไม่หายไปตอนกำลังกรอก
- **Refactor:** เปลี่ยน input เลือกเดือนให้เป็นแบบ Dropdown สองฝั่ง (เดือน / ปี) ทำให้อ่านง่ายและไม่บั๊กในบางเบราว์เซอร์
- **Style:** ปรับความกว้างแนวนอนในหน้าจอให้สุด (Compact styles) เพื่อรองรับหน้าจอ Reports ที่มีคอลัมน์เยอะมากๆ นำไปสู่การเลื่อนหน้าจอน้อยลง

---

## 🚨 ปัญหาคงค้าง / สิ่งที่ควรพิจารณาในอนาคต (Pending Issues / Tech Debt)
> เป็นเป้าหมายที่ตรวจพบจากการอ่านโค้ดครั้งแรก และใช้สำหรับการวางแผนแก้ปัญหาในอนาคต:
1. **Error Recovery ใน `useStore`:** กระบวนการเขียน (Mutations) เป็น Optimistic Update แต่เมื่อ Supabase ผิดพลาด กลับไม่มีการ Revert State ผู้ใช้จะไม่ทราบว่าบันทึกพัง และมีแค่การโยน Log ลง Console
2. **การลบ (Delete Worker) ไม่มี Cascading:** ลบพนักงานแล้ว รายการล็อกต่างๆ (Entries, Advances) ยังค้างเป็น Orphaned Items ก่อปัญหาเวลาออกรีพอร์ต
3. **Advanced Settings Hardcodes:** บางค่าเช่น Guarantee ครึ่งวัน ตอนสร้าง Entry ถูก Hardcode ไว้ที่ `100` อาจจะต้องดึงมาจาก Worker Policy ให้ถูกต้อง 100%

---

## 🔄 กระบวนการอัปเดตเอกสาร (สำหรับ AI)
เมื่อมีการเปลี่ยนแปลง หรือเพิ่มฟีเจอร์ใหม่ ให้ดำเนินการอัปเดตเอกสารนี้ในเซกชั่นด้านล่าง ทุกครั้ง! หรือแก้ไขรายการ Current Features หากเป็นเรื่องใหญ่

---
*(จะเพิ่ม History ของการอัปเดตจากนี้เป็นต้นไปไว้ด้านล่าง)*

### 14 เมษายน 2026: ปรับปรุง Layout ฟอร์มบันทึกรายวัน (Compact Modal)
- **ไฟล์ที่มีการเปลี่ยนแปลง:** 
  - `src/components/ui.tsx` (เพิ่ม prop `maxWidth` ให้ Modal)
  - `src/pages/DailyEntryPage.tsx` (เปลี่ยนฟอร์มให้จัดแบบ Grid 2 คอลัมน์)
- **สิ่งที่อัปเดต:** แก้ปัญหากรอกข้อมูลรายวันแล้วต้องเลื่อนลงยาวเกินความจำเป็น โดยขยายความกว้างของป็อปอัป ดึงฟังก์ชัน "ลางาน" ขึ้นด้านบนสุดแบบเต็มจอ แล้วแยกฝั่งซ้ายเป็น "กลุ่มเวลาทำงาน (เริ่ม/สิ้นสุด/โอที/หักสาย)" และฝั่งขวาเป็น "กลุ่มการเงิน (ค่าแรง/ค่ารถ/ทางด่วน/อื่นๆ)" เพื่อความสมดุล (Balance) ของฟอร์ม
- **ผลกระทบ:** ด้าน UI การแสดงผล Modal แบบใหม่ กว้างขึ้นและสั้นลง ไม่อึดอัด และไม่ต้องกวาดสายตาเลื่อนลงลึกเพื่อกดบันทึกบนจอ Desktop/Tablet (บนหน้าจอมือถือจะยังไหลต่อกันแบบ Responsive)
- **สิ่งที่ต้องทดสอบ:** ทดสอบกดกรอกรายวันบน Desktop (ควรเห็น 2 ฝั่งซ้ายขวา) และบนมือถือ (ควรตอบสนองเรียงลงล่างปกติ) 
- **หมายเหตุ:** ไม่มีผลกระทบดานดาต้าเบส เป็นการปรับแก้ประสบการณ์ผู้ใช้งาน (UX) ล้วนๆ

### 14 เมษายน 2026: แก้ปัญหากล่องฟอร์มยาวเกินเมื่อเพิ่มรายการเยอะ (Scrollable Max-Height)
- **ไฟล์ที่มีการเปลี่ยนแปลง:** `src/pages/DailyEntryPage.tsx`
- **สิ่งที่อัปเดต:** จำกัดความสูงของหน้าต่าง "ทางด่วน" (`max-h-[160px]`) และ "รายการอื่นๆ" (`max-h-[220px]`) พร้อมตกแต่ง UI Scrollbar ให้สามารถเลื่อนขึ้นลงได้อัตโนมัติ 
- **ผลกระทบ:** UI ของป๊อปอัปจะไม่ยืดจนทะลุหน้าจอเมื่อมีการกดเพิ่มรายการมากกว่า 3-4 รายการ ทำให้มองเห็นปุ่ม "บันทึกข้อมูล" ได้เสมอโดยไม่ต้องเสียเวลาเลื่อนหน้าจอหลัก
- **สิ่งที่ต้องทดสอบ:** ลองกดเพิ่มทางด่วน หรือ รายการอื่นๆ จำนวนหลายๆ อัน แล้วลองเลื่อน Scroll ดูว่าใช้งานได้ดีหรือไม่
